    if(EXISTS $ENV{OUTPUT_PB_SOURCE})
        file(READ $ENV{OUTPUT_PB_SOURCE} FILE_CONTENTS)
        string(REPLACE "namespace public {" "namespace public_ {" FILE_CONTENTS "$ENV{FILE_CONTENTS}")
        string(REPLACE "tinkoff::public::" "tinkoff::public_::" FILE_CONTENTS "$ENV{FILE_CONTENTS}")
        file(WRITE $ENV{OUTPUT_PB_SOURCE} "$ENV{FILE_CONTENTS}")
    endif()
    if(EXISTS $ENV{OUTPUT_PB_HEADER})
        file(READ $ENV{OUTPUT_PB_HEADER} FILE_CONTENTS)
        string(REPLACE "namespace public {" "namespace public_ {" FILE_CONTENTS "$ENV{FILE_CONTENTS}")
        string(REPLACE "tinkoff::public::" "tinkoff::public_::" FILE_CONTENTS "$ENV{FILE_CONTENTS}")
        file(WRITE $ENV{OUTPUT_PB_HEADER} "$ENV{FILE_CONTENTS}")
    endif()
    if(EXISTS $ENV{OUTPUT_GRPC_SOURCE})
        file(READ $ENV{OUTPUT_GRPC_SOURCE} FILE_CONTENTS)
        string(REPLACE "namespace public {" "namespace public_ {" FILE_CONTENTS "$ENV{FILE_CONTENTS}")
        string(REPLACE "tinkoff::public::" "tinkoff::public_::" FILE_CONTENTS "$ENV{FILE_CONTENTS}")
        file(WRITE $ENV{OUTPUT_GRPC_SOURCE} "$ENV{FILE_CONTENTS}")
    endif()
    if(EXISTS $ENV{OUTPUT_GRPC_HEADER})
        file(READ $ENV{OUTPUT_GRPC_HEADER} FILE_CONTENTS)
        string(REPLACE "namespace public {" "namespace public_ {" FILE_CONTENTS "$ENV{FILE_CONTENTS}")
        string(REPLACE "tinkoff::public::" "tinkoff::public_::" FILE_CONTENTS "$ENV{FILE_CONTENTS}")
        file(WRITE $ENV{OUTPUT_GRPC_HEADER} "$ENV{FILE_CONTENTS}")
    endif()